include $(TOPDIR)/rules.mk

LUCI_TITLE:=LuCI Support for Shutdown Openwrt
PKG_NAME:=luci-app-shutdown
PKG_VERSION:=20210201
PKG_RELEASE:=1
LUCI_PKGARCH:=all

include $(TOPDIR)/feeds/luci/luci.mk

# call BuildPackage - OpenWrt buildroot signature
